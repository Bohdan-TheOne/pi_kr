<div class="p-2 d-flex justify-content-between">
    <div>
        <strong><%= comment.commenter %>:</strong>
        <%= comment.body %>
    </div>
    <div>
        <%= link_to 'Delete', [comment.article, comment], class: "btn btn-outline-danger",
                method: :delete,
                data: { confirm: "Are you sure?" } %>
    </div>
</div>